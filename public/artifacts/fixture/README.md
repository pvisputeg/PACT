# Free model-path fixture

This fixture exercises the schema-validated plan and Independent Auditor presentation without an API call.

Open `http://127.0.0.1:5173/?artifact=fixture` after `npm run dev`.

The UI labels this input **LOCAL SCHEMA FIXTURE · NO API CALL**. It is not GPT-5.6 output, its IDs are not API response IDs, and the release audit will never accept it as genuine Build Week evidence.
