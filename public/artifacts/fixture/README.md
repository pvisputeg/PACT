# Free Operation Northstar model-path fixture

This fixture exercises the Northstar schema-validated Outcome Lead and independent Auditor presentation without an API call.

Open `http://127.0.0.1:5173/?scenario=northstar&artifact=fixture&reset=1` after `npm run dev`.

The UI labels this input **LOCAL SCHEMA FIXTURE · NO API CALL**. It is not GPT-5.6 output, its IDs are not API response IDs, and the release audit never accepts it as genuine Build Week evidence.
